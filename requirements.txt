# Phonics assessment API (api_server.py)
# Install: pip install -r requirements.txt
#
# Unpinned so pip can choose wheels for your OS/Python (avoids long source builds, e.g. matplotlib).

certifi
fastapi
librosa
matplotlib
numpy
openai
openpyxl
pandas
Pillow
psycopg[binary]
pydantic
pydub
python-dotenv
python-multipart
razorpay
reportlab
requests
sounddevice
soundfile
uvicorn[standard]
# PyAudio omitted — only used in temp.py; requires PortAudio headers to build on Linux (breaks Render).
# sounddevice: lazy-imported in record_and_analyze.start_recording() so API startup does not need PortAudio.
